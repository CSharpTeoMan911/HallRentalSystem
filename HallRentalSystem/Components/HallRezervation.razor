<div class="reservation-div">
    <div class="reservation-content">
        <h2 class="title">Book Hall</h2>

        <div>
            <input class="datetime" type="date" min=@DateTime.Now.Date.ToString("yyyy-MM-dd") @onchange=TextChanged value=@DateTime.Now.Date.ToString("yyyy-MM-dd") />
        </div>

        <div>
            <p>
                Hours
            </p>
            <input type="time" />
            <input type="time" />
        </div>

        <div>
            <p>
                Price
            </p>
        </div>

        <button>
        </button>
    </div>
</div>

@code {
    public string date = DateTime.Now.Date.ToString();
    public delegate void OnClose();

    protected override void OnAfterRender(bool firstRender)
    {
        base.OnAfterRender(firstRender);
    }

    private void TextChanged()
    {
        
    }
}
